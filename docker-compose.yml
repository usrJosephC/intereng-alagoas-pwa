services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: intereng_alagoas
    ports:
      - "5433:5432"
    volumes:
      - intereng_alagoas_pgdata:/var/lib/postgresql/data

volumes:
  intereng_alagoas_pgdata:
